Flask==3.0.2
opencv-python==4.9.0.80
numpy==1.26.4
threading  # Note: threading is part of Python's standard library and does not require installation via pip.
time  # Note: time is part of Python's standard library and does not require installation via pip.
base64  # Note: base64 is part of Python's standard library and does not require installation via pip.
google-generativeai==0.4.1
python-dotenv==1.0.1
websocket-client==1.7.0
requests==2.31.0
PyMuPDF==1.24.0
easyocr==1.7.2
Werkzeug==3.0.1
pyttsx3==2.90
SpeechRecognition==3.14.0
